<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head') %>
  </head>
  <body class="bg-slate-50 text-slate-950">
    <%- include('partials/header') %>
    <main>
      <section class="flex min-h-[calc(100vh-140px)] items-center justify-center px-6 py-24">
        <div class="max-w-3xl rounded-[2rem] border border-slate-200 bg-white px-8 py-16 text-center shadow-2xl">
          <p class="text-sm font-semibold uppercase tracking-[0.3em] text-slate-500">404 — Page Not Found</p>
          <h1 class="mt-6 text-5xl font-serif font-semibold tracking-tight text-slate-950">We couldn’t find that page.</h1>
          <p class="mt-6 text-lg leading-8 text-slate-600">Return to the travel overview and continue exploring destinations.</p>
          <a class="mt-10 inline-flex rounded-full bg-primary px-8 py-4 text-sm font-semibold text-white transition hover:bg-slate-900" href="/">Return Home</a>
        </div>
      </section>
    </main>
    <%- include('partials/footer') %>
  </body>
</html>
